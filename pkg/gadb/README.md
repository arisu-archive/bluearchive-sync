# gadb

This is a fork of the [gadb](https://github.com/electricbubble/gadb) library.

## Changes

- Add `Device.Root()` method to request root access.
- Add `Device.Install()` method to install an APK file.
- Add `Device.InstallMultiple()` method to install multiple APK files.